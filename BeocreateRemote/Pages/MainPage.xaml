<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BeocreateRemote.MainPage"
             xmlns:content="clr-namespace:BeocreateRemote.Content"
             xmlns:viewmodel="clr-namespace:BeocreateRemote.ViewModel"
             x:DataType="viewmodel:MainViewModel">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label Text="{Binding Temperature}" />
            <Button 
                Text="Refresh"
                Command="{Binding RefreshCommand}"
            />
            <Button 
                Text="Mute"
                Command="{Binding MuteCommand}"
            />
            <Button 
                Text="Unmute"
                Command="{Binding UnmuteCommand}"
            />
            <content:AudioView></content:AudioView>
            <Label Text="The End" />
        </VerticalStackLayout>

    </ScrollView>

</ContentPage>
